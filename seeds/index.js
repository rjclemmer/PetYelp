// const sequelize = require('../config/config');
// const seedPet = require('./petData');

// const seedAll = async () => {
//   await sequelize.sync({ force: true });

//   await seedPet();

//   process.exit(0);
// };

// seedAll();
